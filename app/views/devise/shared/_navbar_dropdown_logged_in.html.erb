<% if user_signed_in? %>
  <div class = "align-navbar-right">
    <div class="dropdown">
      <div class="navbar-wagon-item button-try" id="blue" data-toggle="dropdown">
        <%= "logged in"%>
      </div>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li>
          <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
        </li>
        <li>
          <%= link_to('Edit registration', edit_user_registration_path) %>
        </li>
      </ul>
    </div>
  </div>
<% else %>
  <%= link_to('Login', new_user_session_path, class: "navbar-wagon-item button-try ", id: "blue")  %>
  </div>
<% end %>
